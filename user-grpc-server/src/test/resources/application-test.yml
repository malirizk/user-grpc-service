spring:
  config:
    activate:
      on-profile:
        test
  jpa:
    database-platform:
      org.hibernate.dialect.H2Dialect
    show-sql:
      true
    hibernate:
      ddl-auto:
        create
  datasource:
    name: usergrpcdb
    url: jdbc:h2:mem:usertestdb
    driverClassName: org.h2.Driver
    username: sa
    password:
    platform: h2